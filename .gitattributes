# Git LFS removed from .gitattributes

# This repository no longer requires Git LFS at runtime. See MIGRATION.md for a plan
# to permanently remove tracked LFS objects from history if you want to stop using LFS entirely.
